# Domain Docs

Before exploring the codebase, read:

- `CONTEXT.md`
- Relevant ADRs under `docs/adr/`

If these files do not exist, proceed silently.

This is a single-context repository. Use terminology defined in `CONTEXT.md` across issue titles, implementation plans, tests, and code.

If proposed work contradicts an ADR, surface the conflict explicitly rather than silently overriding the decision.
