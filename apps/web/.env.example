# Reference only. Put local values in the repository root .env. The guarded web launcher rejects
# and continuously denies reads of apps/web/.env* files after runtime environment projection.
NEXT_PUBLIC_BASE_SEPOLIA_RPC_URL=
NEXT_PUBLIC_RPC_URL=
NEXT_PUBLIC_DEPLOYMENT_ENVIRONMENT=staging
NEXT_PUBLIC_REOWN_PROJECT_ID=
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://127.0.0.1:8800
